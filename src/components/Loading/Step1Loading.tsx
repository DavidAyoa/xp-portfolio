import React from 'react';
import ContentCenter from '../../layouts/ContentCenter';

const Step1Loading: React.FC = () => {
  return (
    <ContentCenter className="bg-black" />
  );
};

export default Step1Loading;
